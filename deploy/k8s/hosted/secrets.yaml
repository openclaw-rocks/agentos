# Secrets template — fill in values before applying.
# In production, use sealed-secrets or external-secrets-operator.
apiVersion: v1
kind: Secret
metadata:
  name: hosted-api-secrets
type: Opaque
stringData:
  matrix-admin-token: "REPLACE_WITH_MATRIX_ADMIN_TOKEN"
  stripe-secret-key: "REPLACE_WITH_STRIPE_KEY"
---
apiVersion: v1
kind: Secret
metadata:
  name: matrix-registration-token
type: Opaque
stringData:
  token: "REPLACE_WITH_REGISTRATION_TOKEN"
---
apiVersion: v1
kind: Secret
metadata:
  name: s3-backup-credentials
type: Opaque
stringData:
  AWS_ACCESS_KEY_ID: "REPLACE"
  AWS_SECRET_ACCESS_KEY: "REPLACE"
